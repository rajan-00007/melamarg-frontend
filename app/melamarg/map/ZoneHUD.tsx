'use client';

import React from 'react';

// This component is a placeholder to satisfy the planned file structure.
// Currently, active zone details are displayed inside the Advisory Drawer,
// so this component returns null to preserve the exact current UI.
export default function ZoneHUD(props: any) {
  return null;
}
